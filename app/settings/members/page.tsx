// Fix the member invitation functionality by ensuring the component imports and renders correctly
// Make sure all imports are properly resolved and the component structure matches the design

import { WorkspaceMembers } from "@/components/workspace-members"

export default function MembersPage() {
  return <WorkspaceMembers />
}
